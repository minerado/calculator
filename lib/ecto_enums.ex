import EctoEnum

defenum(UserStatusEnum, :user_status, ~w(active inactive)a)

defenum(
  OperationTypeEnum,
  :operation_type,
  ~w(addition subtraction multiplication division square_root random_string)a
)
